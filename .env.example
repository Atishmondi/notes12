# Placeholder configuration only. Do NOT put real secrets here.
# Copy to .env locally if desired (ignored by git), or export directly:
#   export GEMINI_API_KEY="your-key-here"
GEMINI_API_KEY=your-gemini-api-key-here
# Optional model override (code default is gemini-3.6-flash):
# GEMINI_MODEL=gemini-3.6-flash
